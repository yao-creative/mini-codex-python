[project]
name = "mini-codex-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mypy>=2.3.0",
    "ruff>=0.16.2",
]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true  
